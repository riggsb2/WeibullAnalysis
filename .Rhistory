library("ggplot2")
library("reshape2")
filename <- "SampleBreakdownSet.txt"
head(filename)
summary(filename)
head(filename)
summary(filename)
filename <- "\SampleBreakdownSet.txt"
filename <- "/SampleBreakdownSet.txt"
head(filename)
summary(filename)
filename <- SampleBreakdownSet.txt
filename <- SampleBreakdownSet
head(SampleBreakdownSet)
head("SampleBreakdownSet")
head("SampleBreakdownSet.txt")
filename <- "SampleBreakdownSe.csv"
head("SampleBreakdownSet.csv")
d <- read.table(filename)
d <- read.table(filename, header = FALSE, sep = "")
filename <- "SampleBreakdownSet"
d <- read.table(filename, header = FALSE, sep = "")
filename <- "SampleBreakdownSet.txt"
d <- read.table(filename, header = FALSE, sep = "")
head(d)
summary(filename)
d <- read.table(filename, header = FALSE, sep = "")
head(d)
summary(d)
head(d) + tail(d)
d <- read.table(filename, header = FALSE, sep = "")
head(d) + tail(d)
head(d) +
tail(d)
head(d)
tail(d)
t = head(d,1)
t
length(d)
d <- read.table(filename, header = FALSE, sep = "")
head(d)
tail(d)
summary(d)
length(d)
length(t)
length(td)
length(d)
nrow(d)
BDV = tail(d,(nrow(d)-2)
BDV = tail(d,(nrow(d)-2))
BDV = tail(d,(nrow(d)-2))
BDV
head(d)
BDV = tail(d,(nrow(d)-1))
BDV
BDF = BDV/t
BDF = BDV / t
head(t)
t = rep(t,(nrow(d)-1))
t
BDF = BDV / t #t needs to be the same size
t = head(d,1)
t = head(d,1)
t
t = replicate(expr = t, n = (nrow(d)-1))
t
BDF = BDV / t #t needs to be the same size
t = head(d,1)
t
t
BDF = BDV / 2 #t needs to be the same size
BDF
BDF = BDV / t(1:1) #t needs to be the same size
BDF
d <- read.table(filename, header = FALSE, sep = "")
head(d)
tail(d)
summary(d)
nrow(d)
#Extract thickness from the dataset; set to variable t
t = head(d,1)
t
t
#Extract the breakdown voltages and toss into dataset, BDV
BDV = tail(d,(nrow(d)-1))
BDV
#Calculates the breakdown field from the thickness and BDV
BDF = BDV / t(1:1) #t needs to be the same size
BDF
t(1:1)
t = head(d,1)
t
t(1:1)
t[1:1]
BDF = BDV / t[1:1] #t needs to be the same size
BDF = BDV / t[c(1:1] #t needs to be the same size
BDF = BDV / t[c(1:1)] #t needs to be the same size
iris(1:1)
diamonds(1:1)
library("ggplot2")
library("plyr")
library("ggthemes")
library("reshape2")
diamonds(1:1)
iris(1:1)
head(iris)
iris[1:1]
t[1:1]
iris[1,1]
t[1,1]
BDF = BDV / t[1,1] #t needs to be the same size
BDF
